zzpkg_find(OpenCV/4.12.0)
add_executable(image_viewer main.cpp)
target_link_libraries(image_viewer PRIVATE ${OpenCV_LIBS})
if(WIN32)
  target_link_libraries(image_viewer PRIVATE ws2_32)
endif()
zzpkg_copy_required_dlls(image_viewer)

if(MSVC)
  add_subdirectory(debugview_dll)
endif()